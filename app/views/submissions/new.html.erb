<div class="max-w-2xl mx-auto">
  <div class="mb-6">
    <%= link_to group_season_week_path(@group, @season, @week), class: "text-purple-400 hover:text-purple-300 inline-flex items-center" do %>
      <svg class="w-5 h-5 mr-1" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/>
      </svg>
      Back to Week <%= @week.number %>
    <% end %>
  </div>

  <div class="bg-gray-800 rounded-lg p-8 border border-gray-700">
    <h1 class="text-3xl font-bold mb-2">Submit Your Song</h1>
    <p class="text-purple-400 font-semibold text-lg mb-8"><%= @week.category %></p>

    <div class="bg-blue-500/10 border border-blue-500 rounded-lg p-4 mb-6">
      <p class="text-blue-300 text-sm">
        <strong>Deadline:</strong> <%= @week.submission_deadline.strftime("%B %d at %I:%M %p") %>
        <br>
        <strong>Time remaining:</strong> <%= distance_of_time_in_words(Time.current, @week.submission_deadline) %>
      </p>
    </div>

    <% submission_url = @submission.persisted? ? group_season_week_submission_path(@group, @season, @week, @submission) : group_season_week_submissions_path(@group, @season, @week) %>
    <% submission_method = @submission.persisted? ? :patch : :post %>
    <%= form_with model: @submission,
                  url: submission_url,
                  method: submission_method,
                  class: "space-y-6",
                  data: {
                    controller: "tidal-search",
                    action: "submit->tidal-search#validate click->tidal-search#close",
                    "tidal-search-url": search_group_season_week_submissions_path(@group, @season, @week),
                    "tidal-lookup-url": lookup_group_season_week_submissions_path(@group, @season, @week),
                    "tidal-duplicates-url": duplicates_group_season_week_submissions_path(@group, @season, @week),
                    "tidal-submission-id": @submission.id
                  } do |f| %>
      <% if @submission.errors.any? %>
        <div class="bg-red-500/10 border border-red-500 rounded-lg p-4">
          <h3 class="font-semibold text-red-400 mb-2">
            <%= pluralize(@submission.errors.count, "error") %> prevented this submission from being saved:
          </h3>
          <ul class="list-disc list-inside text-red-300">
            <% @submission.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <div class="space-y-3">
        <label for="tidal-query" class="block text-sm font-semibold">Search Tidal</label>
        <div class="relative">
          <input
            id="tidal-query"
            name="tidal_query"
            type="text"
            autocomplete="off"
            class="w-full bg-gray-900 border border-gray-600 rounded-lg px-4 py-3 focus:border-purple-500 focus:ring-1 focus:ring-purple-500"
            placeholder="Start typing a song or artist name"
            value="<%= @submission.song_title.present? ? "#{@submission.song_title} - #{@submission.artist}" : "" %>"
            data-tidal-search-target="input"
            data-action="input->tidal-search#search"
          />
          <div
            id="tidal-suggestions"
            class="absolute z-10 mt-2 w-full bg-gray-900 border border-gray-700 rounded-lg shadow-lg hidden"
            data-tidal-search-target="suggestions"
          ></div>
        </div>
        <p class="text-sm text-gray-400">
          Tidal-only submissions. Choose a track from the suggestions to fill in your submission.
        </p>
        <p id="tidal-error" class="text-sm text-red-400 hidden" data-tidal-search-target="error">
          Select a track from the list before submitting.
        </p>
        <p id="tidal-warning" class="text-sm text-yellow-300 hidden" data-tidal-search-target="warning"></p>
      </div>

      <div class="space-y-3">
        <label for="tidal-url" class="block text-sm font-semibold">Paste a Tidal track link</label>
        <input
          id="tidal-url"
          name="tidal_url"
          type="url"
          autocomplete="off"
          class="w-full bg-gray-900 border border-gray-600 rounded-lg px-4 py-3 focus:border-purple-500 focus:ring-1 focus:ring-purple-500"
          placeholder="https://tidal.com/browse/track/123456789"
          data-tidal-search-target="urlInput"
          data-action="input->tidal-search#lookup"
        />
        <p class="text-sm text-gray-400">
          Paste a full Tidal track link to auto-fill your submission.
        </p>
        <p id="tidal-url-error" class="text-sm text-red-400 hidden" data-tidal-search-target="urlError">
          We couldn’t find that Tidal track link. Double-check the URL.
        </p>
      </div>

      <% selected_classes = @submission.tidal_id.present? ? "border border-gray-700 rounded-lg p-4 bg-gray-900/60" : "hidden border border-gray-700 rounded-lg p-4 bg-gray-900/60" %>
      <div id="tidal-selected" class="<%= selected_classes %>" data-tidal-search-target="selected">
        <div class="hidden">
          <img alt="" data-tidal-search-target="cover" />
        </div>
        <p id="tidal-title" class="text-lg font-semibold" data-tidal-search-target="title">
          <%= @submission.song_title %>
        </p>
        <p id="tidal-artist" class="text-purple-400" data-tidal-search-target="artist">
          <%= @submission.artist %>
        </p>
        <p id="tidal-album" class="text-sm text-gray-400" data-tidal-search-target="album">
          <%= @submission.song_title.present? ? "Unknown album" : "" %>
        </p>
      </div>

      <%= f.hidden_field :song_title, id: "submission-song-title", data: { "tidal-search-target": "songTitleField" } %>
      <%= f.hidden_field :artist, id: "submission-artist", data: { "tidal-search-target": "artistField" } %>
      <%= f.hidden_field :song_url, id: "submission-song-url", data: { "tidal-search-target": "urlField" } %>
      <%= f.hidden_field :tidal_id, id: "submission-tidal-id", data: { "tidal-search-target": "tidalIdField" } %>

      <div class="flex items-center justify-between pt-4">
        <%= f.submit @submission.persisted? ? "Update Song" : "Submit Song",
                     class: "bg-purple-600 hover:bg-purple-700 text-white px-8 py-3 rounded-lg font-semibold cursor-pointer" %>
        <%= link_to "Cancel", group_season_week_path(@group, @season, @week), class: "text-gray-400 hover:text-white" %>
      </div>
    <% end %>
  </div>
</div>
